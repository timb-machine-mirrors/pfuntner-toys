#! /usr/bin/env python

"""
   Keep a terminal awake so that it doesn't time out
"""

import time
import signal
import logging
import datetime

def handler(signum, stack):
  now = datetime.datetime.now()
  log.info('elapsed:{elapsed} done'.format(elapsed=now-start))
  exit(0)
  
logging.basicConfig(format='%(asctime)s %(msg)s')
log = logging.getLogger()
log.setLevel(logging.DEBUG)

start = datetime.datetime.now()
signal.signal(signal.SIGINT, handler)

while True:
  now = datetime.datetime.now()
  log.info('elapsed:{elapsed} zzzzzz'.format(elapsed=now-start))
  time.sleep(5*60)
